on:
  push:
    branches-ignore:    
      - '!main'
    tags:        
      - v*
jobs:
  update-version:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello!"
