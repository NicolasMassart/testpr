on:
  push:
    branches:
      - main
    tags:
      - v*
jobs:
  update-version:
    name: Test
    runs-on: ubuntu-latest
    # prevent wf to run on tags not on main
#    if: github.ref == 'refs/heads/main'
    steps:
      - name: Dump GitHub context
        id: github_context_step
        run: echo '${{ toJSON(github) }}'
